server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/webhook_data
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: kanav007
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com

google:
  client-id: 393356355602-ctoquagsborrhajua676b1eigg657tik.apps.googleusercontent.com

cors:
  allowed-origins: http://localhost:4200

user-service:
  url: http://localhost:8081

gateway:
  base-url: http://localhost:8080
